#!/bin/sh
# =============================================================================
# LucidOS live-build auto/config
# =============================================================================
# This script is executed by lb config from the live-build directory.
# Keep it to options supported by the live-build version used in CI.
# =============================================================================

set -e

lb config noauto \
    --distribution "bookworm" \
    --architectures "amd64" \
    --mirror-bootstrap "http://deb.debian.org/debian/" \
    --mirror-chroot "http://deb.debian.org/debian/" \
    --mirror-binary "http://deb.debian.org/debian/" \
    --keyring-packages "debian-archive-keyring" \
    --archive-areas "main contrib non-free-firmware" \
    --mirror-binary-security "http://security.debian.org/debian-security/" \
    --mirror-chroot-security "http://security.debian.org/debian-security/" \
    --binary-images "iso-hybrid" \
    --bootappend-live "boot=live components quiet splash hostname=lucidos username=lucid locales=en_US.UTF-8 keyboard-layouts=us" \
    --bootappend-install "quiet" \
    --compression "xz" \
    --cache "true" \
    --iso-volume "LucidOS" \
    --iso-application "LucidOS Alpha 0.1" \
    --iso-publisher "LucidOS Project" \
    "${@}"

echo ""
echo "[LucidOS] live-build configuration complete."
echo "[LucidOS] Run 'sudo lb build' to build the ISO."
